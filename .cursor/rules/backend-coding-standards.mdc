---
description: 后端代码规范（提升可维护性）
globs: backend/**/*.{ts,js,py,go,java}
alwaysApply: false
---

# 后端编码规范

- 保持清晰分层：route/controller -> service -> repository。
- 函数尽量短小，业务逻辑与 I/O、框架胶水代码分离。
- 在输入边界做校验，并返回明确的错误信息。
- 不要吞异常；记录上下文后抛出，或映射为领域错误。
- 行为变更需同步新增或更新测试（优先单测，必要时补集成测试）。
- 避免硬编码密钥、地址和魔法值，统一使用配置常量。
- 注释仅用于解释不直观意图，删除冗余注释。
- 优先编写幂等处理逻辑和确定性辅助函数，便于重试。
