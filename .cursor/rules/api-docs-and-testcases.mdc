---
description: API 文档与测试用例编写规范
globs: {docs/**/*.md,**/*.{yaml,yml,json}}
alwaysApply: false
---

# API 文档与测试用例规范

- 每个 API 都要写清：用途、路径、方法、鉴权、请求结构、响应结构。
- 字段名、枚举值、示例必须与实现和 OpenAPI 保持一致。
- 测试用例至少覆盖：成功路径、参数校验失败、鉴权失败、边界输入。
- 每条用例都要明确期望状态码和关键响应断言。
- API 行为发生变化时，文档和测试用例必须在同一改动中同步更新。
- 示例优先使用可复现的具体请求/响应，不使用抽象占位描述。
- 涉及破坏性变更时，需明确标注并补充迁移说明。
